    type RGB = [number, number, number];

        type ColorOptions = {
              displayName: string;
                    rgb: RGB;
                        };

                            export default ColorOptions;

